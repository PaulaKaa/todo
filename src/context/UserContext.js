import { createContext } from 'react'

//e a context, which is used to wrap in all components in React app. It allows components to share same state variables and functions related to user managing.


export const UserContext = createContext()